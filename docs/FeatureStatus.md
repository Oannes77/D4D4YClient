# 4D4Y 客户端 · 功能现状总览

> 更新于 **Sprint 16 之后**（提交 99d81fd）。口径：**只列真实可用的功能**；
> 论坛侧做不到的一律如实标注，不出现「假按钮 / 假数据 / 空列表冒充」。

---

## 一、已可使用（接真实接口或真实系统能力）

### 1. 账号与登录
| 功能 | 状态 | 说明 |
|------|------|------|
| 登录（含安全提问） | ✅ | Discuz `logging.php`，GBK 提交，凭据只进 Keychain |
| 掉线检测 | ✅ | 页面出现「您还未登录」→ 自动标记会话过期，给登录入口 |
| 退出登录 | ✅ | 「我的 → 账号与安全」 |
| 清理缓存 / 关于页 | ✅ | 真实清理本地缓存并显示体积 |
| 权限口径 | ✅ | **游客只浏览能浏览的版块，其余操作一律要求登录** |

### 2. 首页与浏览
| 功能 | 状态 | 说明 |
|------|------|------|
| 板块主题流 | ✅ | 真实 `forumdisplay` + 分页 |
| 点胶囊进板块并刷新 | ✅ | 选中胶囊自动居中（`ScrollViewReader`） |
| 滑胶囊条切换板块 | ✅ | 阈值 60pt 且 > 纵向 1.5 倍，避免误触；到头停住不循环 |
| 板块管理 | ✅ | 置顶 / 收藏 / 排序，决定首页板块顺序 |
| 折叠搜索 + 搜索结果 | ✅ | 复用信息流卡片；搜索需登录，未登录给登录入口 |
| 无限滚动 | ✅ | 分页 URL 来自 `PaginationParser` |
| 单图 / 首图延迟检测 | ✅ | `ImageMetadataRepository`，只测未缓存、上限 20、缓存 24h |
| 被拉黑作者占位 | ✅ | 显示「-已拉黑-」，**不删条目**（删掉会让人以为丢数据） |

### 3. 帖子详情
| 功能 | 状态 | 说明 |
|------|------|------|
| 详情（首帖 + 楼层流） | ✅ | 每页 50 楼 + 页尾分页条 |
| 只看该作者 | ✅ | 「眼睛」按钮，与全部楼层互斥 |
| 长按楼层引用 | ✅ | 带入回复框 |
| 回复提交 | ✅ | `DiscuzFormParser` 运行时解析表单 + GBK + **回读确认** |
| 多图画廊 | ✅ | 全屏左右滑 + 顶部 `n / N` + 单图缩放 |
| 操作栏五键 | ✅ | 回复 / 分享（**菜单：系统分享 + 分享给好友**）/ 收藏（**服务器**）/ 网页版 / **举报** |
| 楼层「-已拉黑-」 | ✅ | 与「该帖已被论坛隐藏」（管理员处罚）**严格区分**，本地拉黑可取消 |

### 4. 发帖
| 功能 | 状态 | 说明 |
|------|------|------|
| 发新帖 | ✅ | 板块选择 + 标题 + 正文；标题留空自动取正文首行 |
| 占位符自动附带 | ✅ | 与正文隔一空行，**不显示在输入框内**（规避最短字数规则） |
| **图片 / 附件上传** | ✅ | `PhotosPicker` + `fileImporter`；**multipart 与正文一次性提交**（文本字段 GBK） |

### 5. 消息与社交
| 功能 | 状态 | 说明 |
|------|------|------|
| 消息 Tab | ✅ | 站内短信 / 系统消息分段 + **真实未读角标** |
| 私信会话 / 发送 | ✅ | 真实 `pm.php`，动态表单解析 + GBK + 回读确认 |
| **加好友 / 删好友** | ✅ | 真实 `my.php?item=buddylist*`；**每次变更后回读好友列表确认** |
| **好友列表** | ✅ | 用户卡进卡先查列表，决定显示「加好友」还是「删好友」 |
| **分享给好友** | ✅ | 选好友 → 站内短信发送标题 + 帖子链接 |
| **举报** | ✅ | 复制帖子链接 + 私信管理员 **4D4Y（UID 29，用户名已确认）**，私信框自动预填「标题 + 链接」草稿（发送仍需用户点一下） |

### 6. 用户
| 功能 | 状态 | 说明 |
|------|------|------|
| 用户卡片 | ✅ | 真实 `space.php`；只列确实取到的字段（UID/分组/帖数/积分），取不到不填假数字 |
| **按作者搜贴** | ✅ | `search.php?srchuid=<uid>&srchfid=all&srchfrom=0` |
| **拉黑 / 取消拉黑** | ✅ | 本地 `BlockedUser`；首页/板块/详情三处统一「-已拉黑-」 |
| 资料读取失败态 | ✅ | 未登录 → 登录入口；结构变化 → 如实说明「读不出来」 |

### 7. 我的
| 功能 | 状态 | 说明 |
|------|------|------|
| 资料框 | ✅ | 登录后显示真实用户名 / UID |
| 宫格 · 帖子 / 回复 / 好友 | ✅ | 真实 `my.php`（动态发现 `item=` 参数） |
| 宫格 · 收藏 | ✅ | **服务器收藏**（`my.php?item=favorites&type=thread`）；未登录给登录入口 |
| 宫格 · 关注 | 🔸 待接入 | 曾按「站点无此栏目」如实说明；**2026-09-20 查明 `my.php?item=attention` 真实存在**，可接真实接口（见 `docs/SiteFacts.md`） |
| 宫格 · 黑名单 | ✅ | 本地屏蔽列表，可在用户卡直接拉黑 / 解除 |
| 设置 · 全部六行 | ✅ | 主题外观 / 回帖占位符 / 显示帖子正文 / 版块管理 / 消息推送 / 账号与安全，**都可点且真生效** |

### 8. 系统能力
| 功能 | 状态 | 说明 |
|------|------|------|
| 后台刷新 | ✅ | 真实 `BGAppRefreshTask`（`com.d4d4y.client.refresh`），未配置时静默降级 |
| 深色模式 | ✅ | 纯黑底 + 紫提亮 #8F86E8 |
| 列表密度 | ✅ | 紧凑 8 / 标准 12 / 宽松 18，列表页统一口径 |
| 相对时间中文化 | ✅ | 今天 09:02 / 昨天 21:04 / 9月18日（客户端自产时间） |
| 截图验收机制 | ✅ | `-DemoScreen=<name>` 直渲目标界面，亮暗各一张 |

---

## 二、未完成 / 待办

| # | 项目 | 类型 | 现状与建议 |
|---|------|------|-----------|
| 1 | 附件上传字段名 | 🔸 待真机验证 | WAP 发帖页（游客看不到）的 file 域名未知，现用兜底 `attach[]`；登录后按真实 HTML 校准 |
| 2 | **「关注」** | 🔸 待接入 | **已查明真实存在**：`my.php?item=attention&action=add&tid=<tid>`（PC 模板实测）。目前「我的 → 关注」按「站点无此栏目」处理，应改为真实接口 |
| 3 | 登录态页面结构 | 🔸 待真机验证 | `my.php` / `pm.php` / `space.php` / `search.php` 游客全部不可达，只能防御式实现 |
| 4 | **详情页内嵌图片** | 🔸 待修 | 列表首图已修（元数据逐请求走 PC 模板）。但**详情正文仍来自 WAP 楼层 HTML**，附件型主题点进去看不到图 → 「列表有缩略图、详情没有」不一致。修法：详情侧补一次 PC 请求 + 决定呈现位置（正文内 / 顶部图片区） |
| 5 | 老附件已被站方清理 | ⚠️ 数据现状 | 2004–2005 年附件实测全部 404（tid=156304 三十张全失效），2006 年后正常。任何方案都取不回，客户端如实不显示 |
| 6 | 图形验证码 | 🚫 暂不支持 | 服务器要求验证码时提示改用浏览器发帖/回复 |
| 7 | 站内评分 | 🚫 站点无 | **两套模板均已复核**：`评分` 计数为 0，确实没有（口径比此前更硬） |

---

## 三、曾经误判、现已纠正的事（留档）

> Sprint 12 结论是「收藏 / 分享被模板注释 ⇒ 站点关闭，做不到」，据此把收藏降级成本地书签、分享降级成系统分享。
> **Sprint 16 由用户提供真实链接后推翻**：入口被注释 ≠ 功能不存在。
> - **收藏真实存在** = 加入 `my.php?item=favorites&type=thread` 自己的收藏列表；
> - **分享 = 站内分享给好友**（站内短信）；
> - 加好友 / 好友列表 / 删好友 / 按作者搜贴 **全部真实存在**；
> - 举报站内无原生入口 → 「复制链接 + 私信管理员」；
> - 评分**确实**做不到（与收藏不同因）。
>
> **2026-09-20 补充 —— 把两次相反的结论同时解释清楚**：站点**按 User-Agent 分发两套模板**。
> Sprint 12 看到「收藏 / 分享被模板整块注释」：那是 **WAP 模板**，也是客户端实际拿到、夹具所保存的那一套，**结论没错**；
> Sprint 16 判「功能真实存在」：依据是 **PC 模板**（桌面 UA 才拿得到）里的 `favoritewin` 弹层，**结论也没错**。
> **两个结论并不矛盾，只是看的不是同一套模板。** 据此已确定权威接口：
> 收藏 `my.php?item=favorites&tid=<tid>`、关注 `my.php?item=attention&action=add&tid=<tid>`。
> 详见 `docs/SiteFacts.md`。
>
> **2026-09-24 再纠正 —— 由用户质疑触发，推翻三条结论**：
> 用户指出「立项时就确认要列表首图预览（Threads 形式），当时说没问题，后来却说要改 PC 模板、工程量很大」。
> 复核后确认**用户是对的**，此前有三处问题：
> 1. **据单个帖子过度概括**：只看 tid=193033 一个帖（WAP 6 张图 / PC 178 张）就写成「WAP 正文配图几乎不存在」。
>    20 个主题抽样实测：WAP 首图可用率 **20%**、PC **50%**、其中真实可下载 **40%** —— 是**覆盖面不足**，不是「不可用」。
> 2. **把懒加载空转误读成「没有数据」**：WAP 页尾的 `attachimgshow(pid)` 要找 `#aimg_<aid>`，
>    而 WAP 页里该元素数为 **0**，属空转；真实地址只在 PC 模板的 `file="…/attachments/…"` 属性上，
>    且**游客可直接下载**（实测 200 image/*，无需登录 / Referer）。
> 3. **夸大改造成本**：说「换 PC 模板 = 整轮重构级」。实际只需**逐请求覆盖 UA**
>    （`HTTPClient.request(path:headers:)` 传 `desktopHeaders`），全站**只有元数据检测这一处**这么用，
>    加上解析器兼容 PC 容器即可 —— 3 个文件 52 行。已修复并验证（提交 `07ed852`）。
> **教训**：① 判断「受模板限制 / 做不到」前必须先做**多板块、多年代抽样**，不能只看一个样本；
> ② 遇到「页面里有调用但结果为空」的情形，要顺着调用链查到**元素是否真的存在**，别停在「页面没渲染」；
> ③ 报工程量前先确认**最小改动面**（全局 vs 单请求）。

---

## 四、下一轮建议（低风险优先）

1. **帖子详情内嵌图片的浏览体验打磨** —— 游客可见、本地可验证、风险最低。
2. **真机登录后复核四处解析**（`my.php` / `pm.php` / `space.php` / `search.php`），把「结构未识别」态按真实 HTML 校准。
3. 若用户愿提供体验账号（只用于看页面结构，**不写代码 / 不提交 git / 不打印凭据**，建议直接给另存 HTML），
   可一次性把 §二 的 1–4 项全部落定。
